__use_yte__: true

name: Variant publication summaries

default-view: Summaries

datasets:
  Summaries:
    path: ?input.csv
    separator: ","
views:
  Summaries:
    dataset: Summaries
    render-table:
      columns:
        Summary:
          custom-path: ?read_file(input.summary_formatter)
        Variant:
          display-mode: normal
        PMIDs:
          display-mode: normal
          plot:
            pills:
              separator: ";"
              range: ["blue"]
