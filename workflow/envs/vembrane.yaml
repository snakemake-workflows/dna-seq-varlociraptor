channels:
  - conda-forge
  - bioconda
dependencies:
  - pysam =0.16
  - python =3.7
  - poetry >=0.12
  - pyyaml =5.3
  - pip:
    - git+https://github.com/FelixMoelder/vembrane.git@vep_support # Replace this by a conda package as soon as it is available